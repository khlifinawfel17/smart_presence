name: smart_presence
description: Application universitaire de gestion de présence des étudiants.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Stockage local (fonctionne sur Android, iOS, Web)
  shared_preferences: ^2.2.2

  # Icônes Material
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
